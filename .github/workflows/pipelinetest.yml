name: Print File

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  read_the_file:
    runs-on: self-hosted
    steps:
      - name: Git clone
        uses: actions/checkout@v2
      - name: Read the file
        run: |
          ls -l